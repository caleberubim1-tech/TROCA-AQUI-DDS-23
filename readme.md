-Clona o projeto 

-Crie o banco com o script da pasta data

-Abra o terminal, entre na pasta server

-'npm i' para instalar os pacotes necessáios 

-crie o arquivo .env com os seguintes dados

PORT = 3000

DB_HOST = localhost
DB_USER = root
DB_PASSAWORD = usbw
DB_NAME = sistema_trocas

JWT_SECRET = 'cr7'
